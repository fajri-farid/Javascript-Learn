const kalimat = "Halo, dunia!";
const sebagian = kalimat.slice(0, 4); // Mengambil 4 karakter pertama
console.log(sebagian); // Output: "Halo"

// Jadi, slice(0, 4) pada array berarti:
// Ambil elemen pada indeks 0 (elemen pertama).
// Ambil elemen pada indeks 1 (elemen kedua).
// Ambil elemen pada indeks 2 (elemen ketiga).
// Ambil elemen pada indeks 3 (elemen keempat).
// Hentikan pengambilan elemen sebelum indeks 4

// kan tadi itu dia isinya hanya satu, yaitu halo, dunia! saja yang mana itu terhitung string
// sehingga index nya itu terhitung perhuruf