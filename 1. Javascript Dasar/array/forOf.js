const names = ["Eko", "Kurniawan", "Khannedy"]; // per index

for (const name of names) {
  console.log(`${name}`);
}

const nama = "Muh. Fajri Farid"; // klw ini dia perhuruf
for (const name of nama) {
  console.log(`${name}`);
}
