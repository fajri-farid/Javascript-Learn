APA ITU SYNCHRONOUS
- Program dalam JavaScript secara default akan dieksekusi baris per baris
- Secara default, proses di JavaScript akan dieksekusi secara Synchronous, artinya baris selanjutnya akan 
  dieksekusi setelah baris sebelumnya selesai dikerjakan
- Proses Synchronous juga biasa disebut Blocking, karena harus menunggu tiap proses selesai, baru proses 
  selanjutnya bisa dilakukan

APA ITU ASYNCHRONOUS
- Asynchronous adalah konsep penting dalam JavaScript yang memungkinkan kode berjalan secara tidak berurutan, 
  sehingga aplikasi bisa tetap responsif dan tidak terhambat oleh tugas yang membutuhkan waktu lama.
- Walaupun secara default proses di JavaScript dieksekusi secara Synchronous, namun kita bisa membuatnya 
  menjadi Asynchronous.
- Berbeda dengan proses Synchronous, pada proses Asynchronous, JavaScript tidak akan menunggu proses 
  tersebut selesai, melainkan JavaScript akan melanjutkan baris selanjutnya, tanpa harus menunggu proses 
  Asynchronous selesai.
- Proses Asynchronous juga biasa disebut Non-Blocking.


PERUMPAMAAN:
Bayangkan kamu sedang memesan makanan di restoran cepat saji. Kamu memesan burger, lalu membayar di kasir. 
Setelah itu, kamu tidak perlu menunggu burger selesai dibuat sambil berdiri di depan kasir. Kamu bisa 
mencari tempat duduk, mengisi minuman, atau bahkan main handphone. Ketika burger sudah siap, namamu akan 
dipanggil dan kamu bisa mengambilnya.


